<UserControl x:Class="CatWorkbookPrismPoc.ToolbarModule.Views.ToolbarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:inf="clr-namespace:CatWorkbookPrismPoc.Infrastructure.Commands;assembly=CatWorkbookPrismPoc.Infrastructure"

             mc:Ignorable="d" >
    <Grid>
        <Menu KeyboardNavigation.TabNavigation="Cycle" HorizontalAlignment="Stretch">
            <MenuItem Header="_File">
                <MenuItem Header="_Open" Command="{x:Static inf:ApplicationCommands.OpenProgram}"/>
                <MenuItem Header="_Save" Command="{Binding SaveCommand}"/>
                <MenuItem Header="E_xit" Command="{Binding ExitCommand}"/>
            </MenuItem>
            <MenuItem Header="_Edit" />
            <MenuItem Header="_Options" />
            <MenuItem Header="_Help" />
        </Menu>

    </Grid>
</UserControl>
